class PrioritiesController < ApplicationController
    # Get endpoints
    # Not sure if the front end has any need to access the priorities table... if it does need to read them, we probably still don't want the front end changing our priorities
end